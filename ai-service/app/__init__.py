"""Smart Query Hub — AI service.

Owns all AI/NLP reasoning: query classification (FR-03), automatic routing
(FR-04), reply drafting (FR-05), and 24h escalation (FR-07). Reads and writes
the SAME PostgreSQL database as the Next.js web layer.
"""

__version__ = "0.1.0"