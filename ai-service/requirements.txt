# Core runtime — installs fast and keeps the service functional without AI keys
# (the pipeline degrades to the deterministic rule-based classifier).
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.43
psycopg[binary]>=3.2.10
pydantic>=2.12
pydantic-settings>=2.6
apscheduler>=3.11,<4
httpx>=0.28
python-dotenv>=1.0

# Dev / test
pytest>=8.3